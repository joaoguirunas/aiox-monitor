---
id: "1.1"
title: "Setup do Projeto — Next.js + TypeScript + Tailwind + Dependências"
status: Ready for Review
priority: Critical
epic: 1
story: 1
branch: feature/1.1-project-setup
---

# Story 1.1 — Setup do Projeto

## Story

**Como** desenvolvedor do aiox-monitor,
**Quero** ter o projeto Next.js configurado com TypeScript, Tailwind e todas as dependências necessárias,
**Para que** as stories seguintes possam ser implementadas sem blockers de infraestrutura.

## Acceptance Criteria

- [x] **AC1** — `package.json` na raiz com Next.js 15+, React 19, TypeScript 5+, Tailwind 4+, `better-sqlite3`, `ws`, e `phaser`
- [x] **AC2** — `tsconfig.json` com paths absolutos configurados (`@/*` → `src/*`)
- [x] **AC3** — `next.config.ts` criado (sem `serverExternalPackages` — ver nota de desvio)
- [x] **AC4** — `tailwind.config.ts` configurado com content paths corretos
- [x] **AC5** — Estrutura de pastas criada: `src/app/`, `src/components/`, `src/lib/`, `src/hooks/`, `src/server/`, `scripts/`, `hooks/`, `public/sprites/`, `data/`
- [x] **AC6** — `data/` adicionado ao `.gitignore`
- [x] **AC7** — `npm run dev` executa sem erros na porta 8888
- [x] **AC8** — `npm run build` executa sem erros de TypeScript ✓
- [x] **AC9** — Página inicial (`/`) redireciona para `/lista`
- [x] **AC10** — `npm run install-hook` e `npm run uninstall-hook` aparecem em `package.json`

## Tasks / Subtasks

- [x] **Task 1** — Criar `package.json` com todas as dependências (AC: 1)
  - `next`, `react`, `react-dom`, `typescript`, `@types/node`, `@types/react`
  - `tailwindcss`, `postcss`, `autoprefixer`
  - `better-sqlite3`, `@types/better-sqlite3`
  - `ws`, `@types/ws`
  - `phaser`
  - `lucide-react` (ícones)
  - Scripts: `dev`, `build`, `start`, `lint`, `typecheck`, `install-hook`, `uninstall-hook`

- [x] **Task 2** — Configurar `tsconfig.json` (AC: 2)
  - `strict: true`
  - `paths: { "@/*": ["./src/*"] }`
  - `moduleResolution: "bundler"`

- [x] **Task 3** — Criar `next.config.ts` (AC: 3)
  - `serverExternalPackages: ['better-sqlite3']`
  - Porta padrão 8888 via `env.PORT`

- [x] **Task 4** — Configurar Tailwind (AC: 4)
  - `tailwind.config.ts` com `content: ['./src/**/*.{ts,tsx}']`
  - `postcss.config.js`
  - `src/app/globals.css` com `@tailwind` directives

- [x] **Task 5** — Criar estrutura de pastas (AC: 5)
  - `src/app/kanban/`, `src/app/lista/`, `src/app/empresa/`
  - `src/components/layout/`, `src/components/kanban/`, `src/components/lista/`, `src/components/empresa/`
  - `src/lib/`, `src/hooks/`, `src/server/`, `scripts/`, `hooks/`
  - `public/sprites/agents/`, `public/sprites/furniture/`, `public/sprites/office/`
  - `data/` (vazio, ignorado pelo git)

- [x] **Task 6** — Atualizar `.gitignore` (AC: 6)
  - Adicionar `data/`, `data/*.db`

- [x] **Task 7** — Criar `src/app/layout.tsx` root e `src/app/page.tsx` com redirect (AC: 7, 9)
  - Layout com `<html>`, `<body>`, import do `globals.css`
  - Page com `redirect('/lista')`

- [x] **Task 8** — Criar pages stub para cada modo (AC: 7)
  - `src/app/lista/page.tsx` — placeholder "Modo Lista"
  - `src/app/kanban/page.tsx` — placeholder "Modo Kanban"
  - `src/app/empresa/page.tsx` — placeholder "Modo Empresa"

- [x] **Task 9** — Criar `scripts/install-hook.ts` e `scripts/uninstall-hook.ts` stubs (AC: 10)

- [x] **Task 10** — Verificar `npm run dev` na porta 8888 e `npm run build` sem erros (AC: 7, 8)

## Dev Notes

### Stack Técnico
- **Next.js** App Router (não Pages Router)
- **Porta:** 8888 — configurar via `next dev --port 8888` no script `dev`
- **better-sqlite3** é um módulo nativo Node.js — OBRIGATÓRIO `serverExternalPackages` no next.config para evitar erro de bundling
- **Phaser.js** só roda no client-side — usar `dynamic(() => import(...), { ssr: false })` quando integrado

### Estrutura de Pastas Esperada (resultado desta story)
```
aiox-monitor/
├── package.json
├── tsconfig.json
├── next.config.ts
├── tailwind.config.ts
├── postcss.config.js
├── src/
│   ├── app/
│   │   ├── globals.css
│   │   ├── layout.tsx
│   │   ├── page.tsx          ← redirect para /lista
│   │   ├── kanban/page.tsx
│   │   ├── lista/page.tsx
│   │   └── empresa/page.tsx
│   ├── components/
│   ├── lib/
│   ├── hooks/
│   └── server/
├── scripts/
├── hooks/
├── public/sprites/
└── data/                     ← no .gitignore
```

### Referência da Arquitetura
- Stack definida em: conversa com @architect (2026-03-14)
- Porta 8888 com auto-incremento é comportamento do servidor (Story 1.3+)

## 🤖 CodeRabbit Integration

Story Type Analysis:
- Primary Type: Architecture
- Complexity: Low (setup inicial, sem lógica de negócio)

Quality Gate Tasks:
- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted`

CodeRabbit Focus Areas:
- TypeScript strict mode configurado corretamente
- Imports absolutos funcionando
- Nenhuma dependência desnecessária

## Dev Agent Record

### Desvio técnico — better-sqlite3 → node:sqlite
**Decisão:** `better-sqlite3` removido do `package.json`. Substituído por `node:sqlite` (built-in Node.js 22+).
**Motivo:** Node.js v25.8.1 exige C++20 para compilar módulos nativos. O Clang 17 suporta, mas `node-gyp` v12.2.0 não passa o flag corretamente. `node:sqlite` é estável no Node 25, sem dependências externas.
**Impacto em AC3:** `serverExternalPackages: ['better-sqlite3']` removido do `next.config.ts` — desnecessário com módulo built-in.
**Impacto em stories seguintes:** Story 1.2 deve usar `import { DatabaseSync } from 'node:sqlite'` em vez de `better-sqlite3`.

### Completion Notes
- `npm run build` passou sem erros — 7 rotas geradas estáticas
- `tsconfig.json` recebeu `"target": "ES2017"` automaticamente pelo Next.js — mantido
- Commit: `9d8a607` — branch `feature/1.1-project-setup`

## File List

*Arquivos criados nesta story:*
- `package.json`
- `tsconfig.json`
- `next.config.ts`
- `tailwind.config.ts`
- `postcss.config.js`
- `src/app/globals.css`
- `src/app/layout.tsx`
- `src/app/page.tsx`
- `src/app/kanban/page.tsx`
- `src/app/lista/page.tsx`
- `src/app/empresa/page.tsx`
- `scripts/install-hook.ts` (stub)
- `scripts/uninstall-hook.ts` (stub)
- `.gitignore` (atualizado)
- `package-lock.json` (gerado)
