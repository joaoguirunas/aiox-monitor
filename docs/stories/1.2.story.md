---
id: "1.2"
title: "Schema SQLite + lib/db.ts + lib/schema.ts"
status: Draft
priority: Critical
epic: 1
story: 2
branch: feature/1.2-sqlite-schema
depends_on: "1.1"
---

# Story 1.2 — Schema SQLite + Camada de Dados

## Story

**Como** servidor do aiox-monitor,
**Quero** ter um banco SQLite inicializado automaticamente com schema versionado,
**Para que** todos os eventos, projetos, agentes, terminais, sessões e configuração da empresa possam ser persistidos localmente.

## Acceptance Criteria

- [ ] **AC1** — `data/monitor.db` é criado automaticamente na primeira execução (se não existir)
- [ ] **AC2** — Tabelas criadas: `projects`, `agents`, `terminals`, `sessions`, `events`, `company_config` com todas as colunas do schema definido na arquitetura
- [ ] **AC3** — Todos os índices definidos na arquitetura são criados
- [ ] **AC4** — `lib/db.ts` exporta uma instância singleton do banco (better-sqlite3)
- [ ] **AC5** — `lib/schema.ts` exporta função `initSchema()` que cria tabelas e índices idempotentemente (IF NOT EXISTS)
- [ ] **AC6** — `lib/queries.ts` exporta funções tipadas para as operações básicas de cada tabela
- [ ] **AC7** — `lib/types.ts` exporta interfaces TypeScript para cada entidade (Project, Agent, Terminal, Session, Event)
- [ ] **AC8** — Schema inclui tabela `terminals` com campos: `id`, `project_id`, `pid`, `session_id`, `status`, `first_seen_at`, `last_active`
- [ ] **AC9** — Tabela `events` tem coluna `terminal_id` (FK para terminals)
- [ ] **AC10** — Tabela `sessions` tem coluna `terminal_id` (FK para terminals)
- [ ] **AC11** — Tabela `company_config` é singleton (`CHECK(id = 1)`), com seed row inserida automaticamente no `initSchema()`
- [ ] **AC12** — `lib/types.ts` exporta interface `CompanyConfig` e tipo `ThemeName`
- [ ] **AC13** — `lib/queries.ts` exporta `getCompanyConfig(): CompanyConfig` e `updateCompanyConfig(data): CompanyConfig`

## Tasks / Subtasks

- [ ] **Task 1** — Criar `src/lib/types.ts` com interfaces TypeScript (AC: 7, 12)
  ```typescript
  export interface Project { id: number; name: string; path: string; detected_at: string; last_active: string; }
  export interface Agent { id: number; project_id: number; name: string; display_name?: string; status: 'idle'|'working'|'break'|'offline'; current_tool?: string; last_active: string; }
  export interface Terminal { id: number; project_id: number; pid: number; session_id?: string; status: 'active'|'inactive'; first_seen_at: string; last_active: string; }
  export interface Session { id: number; project_id: number; agent_id?: number; terminal_id?: number; started_at: string; ended_at?: string; event_count: number; status: 'active'|'completed'|'interrupted'; }
  export interface Event { id: number; project_id: number; agent_id?: number; session_id?: number; terminal_id?: number; type: EventType; tool?: string; input_summary?: string; output_summary?: string; duration_ms?: number; raw_payload?: string; created_at: string; }
  export type EventType = 'PreToolUse'|'PostToolUse'|'UserPromptSubmit'|'Stop'|'SubagentStop';
  export type AgentStatus = 'idle'|'working'|'break'|'offline';
  export type ThemeName = 'espacial' | 'moderno' | 'oldschool' | 'cyberpunk';
  export interface CompanyConfig {
    id: 1;  // sempre 1 — singleton
    name: string;
    logo_path: string | null;
    theme: ThemeName;
    ambient_music: 0 | 1;
    idle_timeout_lounge: number;   // segundos desk → lounge
    idle_timeout_break: number;    // segundos lounge → break room
    updated_at: string;
  }
  ```

- [ ] **Task 2** — Criar `src/lib/db.ts` com singleton better-sqlite3 (AC: 4)
  - Importar `better-sqlite3`
  - Resolver path do `data/monitor.db` relativo à raiz do projeto
  - Criar diretório `data/` se não existir (usando `fs.mkdirSync`)
  - Exportar instância singleton `db`
  - Chamar `initSchema(db)` na inicialização

- [ ] **Task 3** — Criar `src/lib/schema.ts` com `initSchema()` (AC: 2, 3, 5, 11)
  - Criar todas as tabelas com `CREATE TABLE IF NOT EXISTS`
  - Tabela `projects`: id, name, path UNIQUE, detected_at, last_active
  - Tabela `agents`: id, project_id, name, display_name, status CHECK, current_tool, last_active + UNIQUE(project_id, name)
  - Tabela `terminals`: id, project_id, pid, session_id, status CHECK, first_seen_at, last_active + UNIQUE(project_id, pid)
  - Tabela `sessions`: id, project_id, agent_id, terminal_id, started_at, ended_at, event_count, status CHECK
  - Tabela `events`: id, project_id, agent_id, session_id, terminal_id, type CHECK, tool, input_summary, output_summary, duration_ms, raw_payload, created_at
  - Tabela `company_config`: singleton com CHECK(id = 1), seed row com `INSERT OR IGNORE INTO company_config (id) VALUES (1)`
  - Criar todos os índices definidos na arquitetura

- [ ] **Task 4** — Criar `src/lib/queries.ts` com funções tipadas (AC: 6, 13)
  - `upsertProject(path, name): Project`
  - `upsertAgent(projectId, name, displayName?): Agent`
  - `upsertTerminal(projectId, pid, sessionId?): Terminal`
  - `createSession(projectId, agentId?, terminalId?): Session`
  - `closeSession(sessionId): void`
  - `insertEvent(data: Omit<Event, 'id' | 'created_at'>): Event`
  - `getProjects(): Project[]`
  - `getAgents(projectId?: number): Agent[]`
  - `getEvents(filters?: EventFilters): Event[]`
  - `getTerminalsByProject(projectId: number): Terminal[]`
  - `getCompanyConfig(): CompanyConfig`
  - `updateCompanyConfig(data: Partial<Omit<CompanyConfig, 'id' | 'updated_at'>>): CompanyConfig`

- [ ] **Task 5** — Testar criação do banco (AC: 1, 2, 3)
  - Deletar `data/monitor.db` se existir
  - Rodar `npm run dev`
  - Verificar que `data/monitor.db` foi criado
  - Verificar tabelas via `sqlite3 data/monitor.db ".tables"`

## Dev Notes

### Schema SQL Completo (fonte: arquitetura aprovada @architect 2026-03-14)

```sql
-- projects
CREATE TABLE IF NOT EXISTS projects (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    path TEXT NOT NULL UNIQUE,
    detected_at TEXT NOT NULL DEFAULT (datetime('now')),
    last_active TEXT NOT NULL DEFAULT (datetime('now'))
);

-- agents
CREATE TABLE IF NOT EXISTS agents (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    name TEXT NOT NULL,
    display_name TEXT,
    status TEXT NOT NULL DEFAULT 'idle' CHECK(status IN ('idle','working','break','offline')),
    current_tool TEXT,
    last_active TEXT NOT NULL DEFAULT (datetime('now')),
    UNIQUE(project_id, name)
);

-- terminals (suporte a múltiplos terminais por projeto)
CREATE TABLE IF NOT EXISTS terminals (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    pid INTEGER NOT NULL,
    session_id TEXT,
    status TEXT NOT NULL DEFAULT 'active' CHECK(status IN ('active','inactive')),
    first_seen_at TEXT NOT NULL DEFAULT (datetime('now')),
    last_active TEXT NOT NULL DEFAULT (datetime('now')),
    UNIQUE(project_id, pid)
);

-- sessions
CREATE TABLE IF NOT EXISTS sessions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    agent_id INTEGER REFERENCES agents(id) ON DELETE SET NULL,
    terminal_id INTEGER REFERENCES terminals(id) ON DELETE SET NULL,
    started_at TEXT NOT NULL DEFAULT (datetime('now')),
    ended_at TEXT,
    event_count INTEGER DEFAULT 0,
    status TEXT DEFAULT 'active' CHECK(status IN ('active','completed','interrupted'))
);

-- events
CREATE TABLE IF NOT EXISTS events (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    agent_id INTEGER REFERENCES agents(id) ON DELETE SET NULL,
    session_id INTEGER REFERENCES sessions(id) ON DELETE SET NULL,
    terminal_id INTEGER REFERENCES terminals(id) ON DELETE SET NULL,
    type TEXT NOT NULL CHECK(type IN ('PreToolUse','PostToolUse','UserPromptSubmit','Stop','SubagentStop')),
    tool TEXT,
    input_summary TEXT,
    output_summary TEXT,
    duration_ms INTEGER,
    raw_payload TEXT,
    created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

-- company_config (singleton — suporte ao Modo Empresa, Fase 3+)
-- CHECK(id = 1) garante que nunca existirá mais de uma row
CREATE TABLE IF NOT EXISTS company_config (
    id                   INTEGER PRIMARY KEY CHECK(id = 1),
    name                 TEXT NOT NULL DEFAULT 'Minha Empresa',
    logo_path            TEXT,
    theme                TEXT NOT NULL DEFAULT 'moderno'
                         CHECK(theme IN ('espacial','moderno','oldschool','cyberpunk')),
    ambient_music        INTEGER NOT NULL DEFAULT 0,
    idle_timeout_lounge  INTEGER NOT NULL DEFAULT 300,
    idle_timeout_break   INTEGER NOT NULL DEFAULT 300,
    updated_at           TEXT NOT NULL DEFAULT (datetime('now'))
);

-- Seed row: garante que a config existe desde o primeiro boot
INSERT OR IGNORE INTO company_config (id) VALUES (1);
```

### Cuidados com better-sqlite3
- É SÍNCRONO por design — não usar async/await nas queries
- `db.prepare()` retorna um Statement reutilizável — preparar uma vez, executar N vezes
- Para upsert: `INSERT OR REPLACE INTO` ou `INSERT INTO ... ON CONFLICT(col) DO UPDATE SET ...`
- Path do banco deve ser absoluto: `path.join(process.cwd(), 'data', 'monitor.db')`

### Cuidado com Next.js Server Components
- `lib/db.ts` só pode ser importado em Server Components ou Route Handlers
- NUNCA importar `lib/db.ts` em Client Components (`'use client'`)

## 🤖 CodeRabbit Integration

Story Type Analysis:
- Primary Type: Database + Architecture
- Complexity: Medium

Quality Gate Tasks:
- [ ] Pre-Commit (@dev): Verificar schema completo, índices, tipos TypeScript

CodeRabbit Focus Areas:
- Todas as tabelas com CHECK constraints corretos
- UNIQUE constraints em (project_id, name) e (project_id, pid)
- Singleton do banco não vaza entre requests no Next.js
- Tipos TypeScript alinhados com schema SQL
- `company_config` com CHECK(id = 1) e seed row correta
- `ThemeName` como union type, não string livre

## File List

*Arquivos a serem criados nesta story:*
- `src/lib/types.ts`
- `src/lib/db.ts`
- `src/lib/schema.ts`
- `src/lib/queries.ts`

*Nota: `company_config` é criada aqui mas usada ativamente na Fase 3 (Modo Empresa + Settings). A Fase 1 só precisa que a tabela exista e a seed row seja inserida.*
