---
id: "3.6"
title: "Mesa Central Dinâmica — Posições por Agente Ativo"
status: Ready for Review
priority: High
epic: 3
story: 6
branch: feature/3.6-mesa-central-dinamica
depends_on: "3.5"
---

# Story 3.6 — Mesa Central Dinâmica

## Story

**Como** aiox-monitor,
**Quero** que os agentes sejam posicionados dinamicamente no escritório com base no seu status real (vindo do banco de dados),
**Para que** o Modo Empresa reflita o estado real dos agentes sem depender de dados hardcoded.

## Acceptance Criteria

- [x] **AC1** — `src/game/managers/AgentManager.ts` gere o ciclo de vida dos sprites de agentes (criar, destruir, reposicionar)
- [x] **AC2** — Na inicialização da cena, o `AgentManager` faz fetch de `/api/agents` e cria um `AgentSprite` por agente ativo (status != offline)
- [x] **AC3** — Agentes com status `working` são posicionados em mesas da zona de trabalho (uma mesa por agente, alocação sequencial)
- [x] **AC4** — Agentes com status `idle` são posicionados na zona lounge (nos sofás)
- [x] **AC5** — Agentes com status `break` são posicionados na zona lounge (perto da mesa de café)
- [x] **AC6** — Agentes com status `offline` não são renderizados (ou ficam na zona de entrada e desaparecem)
- [x] **AC7** — A mesa ocupada tem tela acesa com a cor do agente que a ocupa
- [x] **AC8** — `AgentManager` mantém um mapa `agentName → deskIndex` para rastrear alocação de mesas
- [x] **AC9** — `src/game/bridge/react-phaser-bridge.ts` expõe funções para React comunicar com a cena Phaser (push de dados para o jogo)
- [x] **AC10** — `PhaserGame.tsx` passa dados iniciais dos agentes via bridge na montagem do componente
- [x] **AC11** — O agente de teste hardcoded da Story 3.4 é removido — agentes vêm apenas do fetch
- [x] **AC12** — `npm run typecheck` e `npm run build` passam sem erros

## Tasks / Subtasks

- [x] **Task 1** — Criar `src/game/bridge/react-phaser-bridge.ts` (AC: 9)
  ```typescript
  import type Phaser from 'phaser';
  import type { Agent } from '@/lib/types';

  // Referência global ao game instance para React→Phaser communication
  let gameInstance: Phaser.Game | null = null;

  export function setGameInstance(game: Phaser.Game): void {
    gameInstance = game;
  }

  export function getGameInstance(): Phaser.Game | null {
    return gameInstance;
  }

  // Eventos customizados via EventEmitter do Phaser
  export function emitToGame(event: string, data: unknown): void {
    const scene = gameInstance?.scene.getScene('OfficeScene');
    if (scene) {
      scene.events.emit(event, data);
    }
  }

  // Evento: sincronizar lista completa de agentes
  export function syncAgents(agents: Agent[]): void {
    emitToGame('sync:agents', agents);
  }

  // Evento: atualizar um agente individual
  export function updateAgent(agent: Agent): void {
    emitToGame('update:agent', agent);
  }

  export function clearGameInstance(): void {
    gameInstance = null;
  }
  ```

- [x] **Task 2** — Criar `src/game/managers/AgentManager.ts` (AC: 1, 2, 3, 4, 5, 6, 7, 8)
  ```typescript
  import Phaser from 'phaser';
  import { AgentSprite } from '../objects/AgentSprite';
  import { Desk } from '../objects/Desk';
  import { getAgentColor } from '../data/agent-visuals';
  import {
    DESK_POSITIONS, LOUNGE_POSITIONS, ENTRANCE_POSITION
  } from '../data/office-layout';
  import type { Agent, AgentStatus } from '@/lib/types';

  const DISPLAY_NAMES: Record<string, string> = {
    '@dev': 'Dex', '@qa': 'Quinn', '@architect': 'Aria',
    '@pm': 'Morgan', '@sm': 'River', '@po': 'Pax',
    '@analyst': 'Alex', '@devops': 'Gage',
    '@data-engineer': 'Dara', '@ux-design-expert': 'Uma',
    '@aiox-master': 'AIOX',
  };

  export class AgentManager {
    private sprites: Map<string, AgentSprite> = new Map();
    private deskAssignment: Map<string, number> = new Map(); // agentName → deskIndex
    private nextFreeDeskIndex = 0;

    constructor(
      private scene: Phaser.Scene,
      private desks: Desk[],
    ) {}

    // Sincroniza lista completa de agentes (chamado na init)
    syncAll(agents: Agent[]): void {
      const activeNames = new Set<string>();

      for (const agent of agents) {
        activeNames.add(agent.name);
        const existing = this.sprites.get(agent.name);

        if (existing) {
          this.updateAgentPosition(agent.name, agent.status);
        } else if (agent.status !== 'offline') {
          this.createAgent(agent);
        }
      }

      // Remover sprites de agentes que não estão mais na lista
      for (const [name, sprite] of this.sprites) {
        if (!activeNames.has(name)) {
          this.removeAgent(name);
        }
      }
    }

    // Cria sprite e posiciona baseado no status
    private createAgent(agent: Agent): void {
      const startPos = ENTRANCE_POSITION;
      const displayName = agent.display_name ?? DISPLAY_NAMES[agent.name] ?? agent.name;
      const sprite = new AgentSprite(
        this.scene, agent.name, displayName,
        startPos.tileX, startPos.tileY,
      );
      sprite.setStatus(agent.status);
      this.sprites.set(agent.name, sprite);

      // Posicionar no local correto
      this.positionAgent(agent.name, agent.status);
    }

    // Atualiza posição de um agente existente
    updateAgent(agent: Agent): void {
      const existing = this.sprites.get(agent.name);
      if (!existing && agent.status !== 'offline') {
        this.createAgent(agent);
        return;
      }
      if (existing) {
        existing.setStatus(agent.status);
        this.updateAgentPosition(agent.name, agent.status);
      }
    }

    private updateAgentPosition(name: string, newStatus: AgentStatus): void {
      const sprite = this.sprites.get(name);
      if (!sprite) return;

      if (newStatus === 'offline') {
        this.removeAgent(name);
        return;
      }

      this.positionAgent(name, newStatus);
    }

    private positionAgent(name: string, status: AgentStatus): void {
      const sprite = this.sprites.get(name);
      if (!sprite) return;

      if (status === 'working') {
        const deskIdx = this.assignDesk(name);
        if (deskIdx >= 0 && deskIdx < DESK_POSITIONS.length) {
          const desk = DESK_POSITIONS[deskIdx];
          sprite.setTilePosition(desk.tileX, desk.tileY);
          sprite.sitDown();
          this.scene.time.delayedCall(400, () => sprite.startTyping());
          // Acender tela da mesa
          if (deskIdx < this.desks.length) {
            this.desks[deskIdx].setScreenOn(getAgentColor(name));
          }
        }
      } else if (status === 'idle' || status === 'break') {
        this.releaseDesk(name);
        // Posicionar no lounge
        const loungeIdx = status === 'break' ? 2 : // perto da mesa café
          [...this.sprites.keys()].filter(n => {
            const s = this.sprites.get(n);
            return s && n !== name;
          }).length % LOUNGE_POSITIONS.length;
        const pos = LOUNGE_POSITIONS[Math.min(loungeIdx, LOUNGE_POSITIONS.length - 1)];
        sprite.setTilePosition(pos.tileX, pos.tileY);
        sprite.standIdle();
      }
    }

    private assignDesk(name: string): number {
      // Se já tem mesa atribuída, manter
      const existing = this.deskAssignment.get(name);
      if (existing !== undefined) return existing;

      // Encontrar próxima mesa livre
      const usedDesks = new Set(this.deskAssignment.values());
      for (let i = 0; i < DESK_POSITIONS.length; i++) {
        if (!usedDesks.has(i)) {
          this.deskAssignment.set(name, i);
          return i;
        }
      }
      return -1; // sem mesas livres
    }

    private releaseDesk(name: string): void {
      const deskIdx = this.deskAssignment.get(name);
      if (deskIdx !== undefined) {
        this.deskAssignment.delete(name);
        if (deskIdx < this.desks.length) {
          this.desks[deskIdx].setScreenOff();
        }
      }
    }

    private removeAgent(name: string): void {
      const sprite = this.sprites.get(name);
      if (sprite) {
        sprite.destroy();
        this.sprites.delete(name);
      }
      this.releaseDesk(name);
    }
  }
  ```

- [x] **Task 3** — Atualizar `OfficeScene.ts` — usar AgentManager + bridge events (AC: 2, 11)
  ```typescript
  import { AgentManager } from '../managers/AgentManager';
  import type { Agent } from '@/lib/types';

  private agentManager!: AgentManager;

  create(): void {
    this.drawFloor();
    this.placeFurniture();
    this.setupCamera();

    this.agentManager = new AgentManager(this, this.desks);

    // Escutar eventos do bridge (React→Phaser)
    this.events.on('sync:agents', (agents: Agent[]) => {
      this.agentManager.syncAll(agents);
    });

    this.events.on('update:agent', (agent: Agent) => {
      this.agentManager.updateAgent(agent);
    });
  }
  // Remover placeTestAgent() — agentes vêm do bridge
  ```

- [x] **Task 4** — Atualizar `PhaserGame.tsx` — fetch inicial + bridge (AC: 10)
  ```typescript
  import {
    setGameInstance, clearGameInstance, syncAgents
  } from '@/game/bridge/react-phaser-bridge';

  useEffect(() => {
    const config = createGameConfig('phaser-container');
    const game = new Phaser.Game(config);
    gameRef.current = game;
    setGameInstance(game);

    // Fetch agentes e sincronizar quando OfficeScene estiver pronta
    game.events.on('ready', async () => {
      try {
        const res = await fetch('/api/agents');
        const agents = await res.json();
        // Aguardar OfficeScene estar activa
        const waitForScene = setInterval(() => {
          const scene = game.scene.getScene('OfficeScene');
          if (scene && scene.scene.isActive()) {
            clearInterval(waitForScene);
            syncAgents(agents);
          }
        }, 100);
      } catch { /* silent — scene starts empty */ }
    });

    return () => {
      clearGameInstance();
      game.destroy(true);
      gameRef.current = null;
    };
  }, []);
  ```

- [x] **Task 5** — Validar posicionamento dinâmico (AC: 12)
  ```bash
  npm run typecheck
  npm run build
  # 1. Enviar eventos curl para criar agentes working/idle
  curl -X POST http://localhost:8888/api/events \
    -H "Content-Type: application/json" \
    -d '{"hook_type":"PreToolUse","project_path":"/tmp/test","agent_name":"@dev","tool_name":"Read"}'

  curl -X POST http://localhost:8888/api/events \
    -H "Content-Type: application/json" \
    -d '{"hook_type":"PreToolUse","project_path":"/tmp/test","agent_name":"@qa","tool_name":"Bash"}'

  # 2. Abrir /empresa → @dev e @qa sentados em mesas, telas acesas
  # 3. Enviar Stop para @dev → deve ir para lounge (idle)
  curl -X POST http://localhost:8888/api/events \
    -H "Content-Type: application/json" \
    -d '{"hook_type":"Stop","project_path":"/tmp/test","agent_name":"@dev"}'
  # 4. Verificar: @dev no lounge, mesa 0 tela apagada
  ```

## Dev Notes

### React → Phaser bridge

O padrão é: React tem os dados (via hooks, fetch, WS), Phaser é o renderizador. O bridge permite que React "empurre" dados para a cena Phaser via `emitToGame()`, que usa o sistema de eventos do Phaser.

Não usar polling do Phaser para buscar dados — o React é o dono do estado.

### Alocação de mesas

Alocação sequencial: primeiro agente `working` vai para desk-0, segundo para desk-1, etc. Se um agente sai (idle/break), a mesa é liberada e fica disponível para o próximo. O mapa `deskAssignment` mantém a atribuição.

### Display names

O `AgentManager` usa `agent.display_name` do banco (se disponível) ou o mapa `DISPLAY_NAMES` como fallback. Consistente com `agent-tracker.ts`.

### Scene lifecycle

O `OfficeScene.create()` registra os listeners. O `PhaserGame.tsx` aguarda que a scene esteja activa (`scene.isActive()`) antes de enviar dados. Isto garante que o `AgentManager` existe quando os dados chegam.

### Mesas vs agentes

Se houver mais de 11 agentes working simultaneamente (improvável), os excedentes ficam na zona de entrada em pé. O assignDesk retorna -1 e o agente fica no local default.

## 🤖 CodeRabbit Integration

Story Type Analysis:
- Primary Type: Integration (React↔Phaser bridge)
- Secondary Type: Game logic (agent positioning)
- Complexity: High

Quality Gate Tasks:
- [ ] Pre-Commit (@dev): Verificar alocação/liberação de mesas, bridge events, fetch inicial, cleanup

CodeRabbit Focus Areas:
- Memory leak: event listeners removidos no cleanup
- Bridge global reference: `clearGameInstance()` chamado no unmount
- Desk assignment/release: sem mesas "presas" após agent offline
- Interval cleanup: `clearInterval(waitForScene)` sempre executado
- Race condition: OfficeScene pode não existir quando fetch completa

## Dev Agent Record

### Completion Notes
- `react-phaser-bridge.ts`: bridge com `setGameInstance`, `syncAgents`, `updateAgent`, `emitToGame` — React como dono do estado, Phaser como renderizador
- `AgentManager.ts`: ciclo de vida completo — `syncAll()` para carga inicial, `updateAgent()` para updates individuais, `assignDesk`/`releaseDesk` para alocação de mesas
- Working → mesa com tela acesa na cor do agente; idle → lounge/sofás com standIdle; break → perto da mesa de café; offline → sprite destruído
- OfficeScene: removido `placeTestAgent()`, adicionados listeners `sync:agents` e `update:agent`
- PhaserGame.tsx: fetch `/api/agents` no `ready` event, aguarda OfficeScene activa via interval antes de chamar `syncAgents`
- `npm run typecheck` e `npm run build` passam sem erros

### Agent Model Used
Claude Opus 4.6

## File List

*Arquivos criados/modificados nesta story:*
- `src/game/bridge/react-phaser-bridge.ts` (criado)
- `src/game/managers/AgentManager.ts` (criado)
- `src/game/scenes/OfficeScene.ts` (atualizado — AgentManager + bridge events, remove placeTestAgent)
- `src/components/empresa/PhaserGame.tsx` (atualizado — fetch + bridge)
